class Config {
  String name = '';
  int timeout = 0;
  final List<String> flags = [];

  void enable(String flag) => flags.add(flag);
}

Config defaults() {
  return Config()
    ..name = 'app'
    ..enable('verbose')
    ..timeout = 30
    ..enable('color');
}
