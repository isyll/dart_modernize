import 'dart:convert';

String encode(Token t) => jsonEncode(t.value);

Token parse(int code) {
  switch (code) {
    case 0:
      return .new('zero');
    default:
      return .new('other');
  }
}

class Token {
  final String value;

  Token(this.value);
}
