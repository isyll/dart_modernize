import 'dart:convert';

String encode(Token t) => jsonEncode(t.value);

Token parse(int code) {
  return switch (code) {
    0 => Token('zero'),
    _ => Token('other'),
  };
}

class Token {
  final String value;

  Token(this.value);
}
