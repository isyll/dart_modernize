import 'dart:convert';

class Token {
  final String value;

  Token(this.value);
}

Token parse(int code) {
  return switch (code) {
    0 => .new('zero'),
    _ => .new('other'),
  };
}

String encode(Token t) => jsonEncode(t.value);
